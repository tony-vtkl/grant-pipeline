# Grant Ingestion Pipeline Configuration
# Per INTAKE BLOCK 1 DoD: README documents env vars

# Required: SAM.gov API key (per INTAKE BLOCK 1)
SAM_API_KEY=SAM-4bd94da0-aa58-4422-a387-93f954c86e40

# Required: Supabase PostgreSQL connection URL
# Format: postgresql://postgres:[PASSWORD]@[HOST]:6543/postgres
DATABASE_URL=postgresql://postgres:your-password@db.example.supabase.co:6543/postgres

# Optional: Anthropic API key (for future REQ-3 scoring)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: Grants.gov attribution header (per ToS)
GRANTS_GOV_ATTRIBUTION=VTKL Grant Pipeline

# Optional: Polling interval in minutes (default: 60)
POLLING_INTERVAL_MINUTES=60

# Optional: Log level
LOG_LEVEL=INFO
